# Remove LFS tracking for video files to avoid installation issues
# These demo videos are documentation-only and not needed for the CLI package
# If needed, they can be hosted elsewhere (e.g., GitHub releases, external hosting)

